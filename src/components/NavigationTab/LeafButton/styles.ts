import { StyleSheet } from "react-native";
import { Colors } from "../../../configs/colors";

export const styles = StyleSheet.create({
    leafButton: {
        width: 47,
        height: 47,
        backgroundColor: Colors.green_android,
        borderRadius: 30,
        justifyContent: 'center',
        alignItems: 'center'
    },

    leafIcon: { 
        width: 22, 
        height: 23.5
    }
})